name = "kommo-mcp-server"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

# Wrangler faz build automático do TypeScript

[vars]
# Variáveis públicas (não sensíveis)
# KOMMO_DEBUG = "false"

# Para variáveis sensíveis, use:
# wrangler secret put KOMMO_BASE_URL
# wrangler secret put KOMMO_ACCESS_TOKEN
